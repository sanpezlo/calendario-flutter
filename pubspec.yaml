name: calendario_flutter
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.3.4 <4.0.0"

dependencies:
  firebase_core: ^2.30.1
  firebase_messaging: ^14.9.0
  flutter:
    sdk: flutter
  flutter_neumorphic_plus: ^3.3.0
  image_stack: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
